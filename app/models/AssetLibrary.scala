package models

import play.api._

// a library of assets
object AssetLibrary {

    // load the library from the given path
    def load(path: String) {
        Logger.debug("Loading AssetLibrary from " + path)
    }

    // asset queries
}